# Task: [short name]

## Goal
What this workflow must do, in one or two sentences.

## Trigger
- Type: Webhook (POST) / Schedule / Manual
- Path (if webhook): james-<name>
- Expected input JSON:
```json
{}
```

## Nodes / flow
Describe the intended node chain, e.g. Webhook → HTTP Request → Respond.

## Credentials
Reference n8n credential NAMES only. Never put keys in this repo.
- e.g. "Header Auth: n8n API Key"

## Output / success criteria
What James will test once the workflow is active (webhook URL, expected response).

## Notes
Constraints, edge cases, things to avoid.
