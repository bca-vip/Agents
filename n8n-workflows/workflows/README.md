# workflows/

Finished, importable n8n workflow JSON built by Claude Code lives here.

Naming: `<webhook-or-purpose>.json` (e.g. `james-run-template.json`).

Each file must be a complete n8n workflow export (nodes, connections, settings) with credentials referenced by name only — never embed keys.
